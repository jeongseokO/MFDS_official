# Python 3.10 is recommended.
# vLLM and torch builds are CUDA/cluster-specific; if your cluster provides
# prebuilt modules, install those first and then install the remaining packages.

torch==2.9.0
transformers==4.57.1
tokenizers==0.22.2
accelerate==1.12.0
vllm==0.11.2
flashinfer-python==0.5.2
peft==0.18.1
safetensors==0.7.0
sentencepiece==0.2.1
huggingface-hub==0.36.1
hf-xet==1.2.0

gradio==6.11.0
fastapi==0.128.0
uvicorn==0.40.0
python-dotenv==1.2.1
requests==2.32.5

faiss-cpu==1.13.2
langchain-community==0.4.1
langchain-huggingface==1.2.0
sentence-transformers==5.2.2
rank-bm25==0.2.2

numpy==1.26.4
scipy==1.15.3
pandas==2.3.3
tqdm==4.67.2
regex==2026.1.15
pydantic==2.12.5
PyMuPDF==1.27.2.2
pillow==12.1.0

# Local PDF OCR pipeline.
# Native executables such as tesseract, ghostscript, qpdf, unpaper, and poppler
# must be installed separately, preferably from conda-forge or the OS package
# manager. Keep OCRmyPDF installed from pip to avoid stale conda solver
# combinations such as old OCRmyPDF with newer pikepdf.
ocrmypdf==16.13.0
pikepdf==10.6.0
img2pdf==0.6.3
pdfminer.six==20260107
cryptography==47.0.0
cffi==2.0.0
pi-heif==1.3.0
pluggy==1.6.0
deprecation==2.1.0
